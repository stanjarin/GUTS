GUTS — FORMAT 1 — COMPLETE SIX

Purpose: first actual reader-format/pagination pass. No sockets or force paragraphs added.

Formatting doctrine:
- Mobile-reader target: nominal 390 × 844 CSS px.
- Comfortable book typography target: Georgia-equivalent 19 px, ~1.5 line height.
- Structural divisions are explicit and page-starting. DR: 47 story heads; three collection labels remain secondary hierarchy. JJ: 18 GUTS navigation episode heads, explicitly navigation rather than claimed original Joyce chapter titles.
- Exceptionally massive authorial paragraphs are split at sentence boundaries into manageable performance chunks. Wording and sequence are unchanged.
- Existing verse/dialogue structure is preserved where present.
- Pagination is deterministic engineering pagination for GUTS; FORMAT1 checker PDFs expose page boundaries for visual QA.
- NO $$$ sockets in this pass.

Files per book:
*_PAGES.json = machine-readable reader corpus.
*_PAGED.txt = page-numbered human-readable corpus.
*_CHECKER.pdf = visual checking copy.

Next after FORMAT1 QA: bring in the three inherited NoBo books, normalize all nine to this reader format, then run socket/force-paragraph machinery.
